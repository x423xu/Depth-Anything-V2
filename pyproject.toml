[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "depth-anything-v2"
version = "0.0.0"
description = "Local editable wrapper for Depth-Anything-V2"
requires-python = ">=3.8"

[tool.setuptools]
packages = ["depth_anything_v2"]
